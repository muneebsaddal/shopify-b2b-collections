# F2 Continuation Memory

**Date:** 2026-07-26
**Parent task:** F2
**Status:** In progress

- Basic-store paid, refunded, cancelled, edited, missing-email, and
  zero-outstanding fixtures passed without retaining protected or identifying
  response data.
- Cancelled orders can remain payment-pending and unpaid while current and
  outstanding balances are zero; cancellation must override those flags for
  actionability.
- The missing `shopify.web.toml` foundation defect was fixed using the official
  React Router frontend/backend web-process configuration and a regression
  test.
- Default Cloudflare QUIC tunneling failed on this network. The bundled
  Cloudflare client succeeded over HTTP/2 and exposed the corrected preview.
- Unsigned webhook requests returned 400 locally and publicly. Shopify CLI
  accepted a signed `app/scopes_update` sample, but the handler did not observe
  it during the test window, so authentic delivery remains pending.
- Planned Shopify `2026-07` webhook topics are documentation-verified under the
  approved scopes but remain unregistered until S1 provides durable handlers.
- Expiring offline-token configuration, session fields, and official library
  refresh support are locally proved; live rotation/concurrency is pending.
- Partner API `activeSubscription` remains the canonical App Pricing authority
  and a `2026-07` release-candidate surface. Runtime proof needs a Manage-apps
  Partner API client and private test plan.
- Do not begin F3 until the remaining F2 external proof is completed or
  explicitly reviewed as a named blocker.
