# F2 Continuation Handoff

**Recorded:** 2026-07-26
**Parent task:** F2 - Link Shopify app and prove the platform contract
**Status:** External proof remains

This record supersedes the 2026-07-20 F2 pause instructions. Do not begin F3
until the remaining F2 proof is completed or resolved through explicit
architecture review.

## Completed

- The Shopify CLI project is linked to the development app.
- The Basic development store grants exactly:
  `read_orders`, `read_all_orders`, `read_payment_terms`, and `read_customers`.
- Shopify Admin API `2026-07` narrow contract queries proved:
  - installation identity and timezone presence;
  - company, location, and contact relationships;
  - email-only protected-field access and missing-email behavior;
  - current and overdue Net terms;
  - paid and zero outstanding;
  - refund;
  - cancellation;
  - edit;
  - multi-currency MoneyBag behavior.
- Cancellation can retain payment-pending/unpaid flags while authoritative
  current and outstanding balances are zero. Cancellation must take precedence
  when determining whether a receivable is actionable.
- Query cost was measured for the narrow receivable contract without retaining
  identifiers, exact amounts, or raw responses.
- The React Router web-process configuration is restored and regression-tested.
- The public webhook route rejects unsigned input with HTTP 400.
- The isolated synthetic PostgreSQL database contains the Shopify `Session`
  table and supports expiring offline-session metadata.
- Live offline-token rotation passed:
  - one expired offline session rotated after an authenticated embedded load;
  - two concurrent authenticated tabs converged to one valid session;
  - both access and refresh values changed;
  - access expiry advanced to approximately 59 minutes;
  - corrected preview logs contained no session/runtime error markers.
- Official documentation verification on 2026-07-26 confirmed:
  - Partner API `2026-07` is the latest documented version;
  - `activeSubscription` is the canonical live Shopify App Pricing query;
  - a Partner API client with Manage-apps permission is required;
  - only an organization owner can create/manage that client;
  - a $0 private test plan is available.

## Validation

The required local gates passed on 2026-07-26:

- `npm test`: 5 files, 11 tests passed.
- `npm run lint`: passed.
- `npm run typecheck`: passed.
- `npm run prisma:validate`: passed with the isolated container
  `DATABASE_URL` injected in process memory only.
- `npm run build`: passed.

The Shopify development preview was stopped after the token proof so it would
not retain a Prisma engine file lock. The synthetic PostgreSQL container
`b2b-ar-f2-postgres` remains the development session database on local port
5433. Its password was never printed or written to the repository.

## Evidence

- `docs/evidence/f2/2026-07-26-checkpoint.md`
- `docs/evidence/f2/2026-07-26-runtime-checkpoint.md`
- `docs/evidence/f2/2026-07-26-webhook-tunnel-blocker.md`
- `docs/evidence/f2/2026-07-26-token-rotation-proof.md`
- `docs/evidence/f2/2026-07-26-official-contract-refresh.md`

No token, refresh token, token digest, HMAC, webhook body, buyer email, shop
domain, Shopify resource ID, exact financial amount, or protected response
payload is retained in this record.

## Remaining external proof

### 1. Authentic webhook delivery

Temporary Cloudflare tunnel delivery returned or resulted in transport failure
before the application handler could prove authentic signed delivery. Use a
durable hosted development endpoint or reliable named tunnel. Then trigger a
real reversible Shopify action, observe the signed `app/scopes_update` or
another approved lifecycle event at the corrected handler, restore the exact
four scopes, and retain sanitized behavior only.

Duplicate and reversed event processing belongs to S1 durable-intake
integration proof, not this F2 route-capability proof.

### 2. Plus compatibility

The linked store is Basic. Create or obtain a Plus development store/sandbox
and prove the Plus-only partial-payment scenario with the narrow
`F2ReceivableOrderContract`.

Do not infer Plus behavior from the Basic store.

### 3. Negative outstanding

Prove a supported synthetic Shopify workflow that produces authoritative
negative outstanding, or record a named blocker and route it through explicit
architecture review. Do not fabricate or infer the state locally.

### 4. Shopify App Pricing runtime states

An organization owner must:

1. Create a Partner API client with Manage-apps permission.
2. Configure the app's $0 private test plan.
3. Use identifiers only at request time.
4. Query `activeSubscription` and scoped event history.
5. Exercise and sanitize evidence for no-active/free, test/active, frozen,
   cancelled, and downgrade or pending-update behavior.

Do not store the Partner API token, app ID, shop ID, raw response, or dashboard
URL in Git.

## Next action order

1. Provision a durable development webhook endpoint.
2. Create the Partner API client and $0 private test plan.
3. Create or obtain the Plus development store.
4. Complete webhook, App Pricing, Plus partial-payment, and
   negative-outstanding proofs.
5. Reconcile `docs/evidence/f2/fixture-matrix.md`,
   `docs/implementation_plan.md`, `docs/phase-tasks.md`, and
   `memory/current-state.md`.
6. Rerun all five local gates.
7. Review F2 acceptance evidence. Keep F2 incomplete if any external proof or
   reviewed blocker is still pending.

## Known workspace issue

The Windows sandbox patch helper can add new files but intermittently fails to
read and update pre-existing files with `helper_unknown_error: apply deny-read
ACLs`. Existing F2 trackers therefore still contain stale 2026-07-20 language.
Use this handoff and the two new 2026-07-26 evidence files as the continuation
source until the patch helper is repaired and the trackers can be reconciled.
