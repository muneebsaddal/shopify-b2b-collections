# Stage 4 testing record — 2026-08-01

## Result

The local Stage 4 campaign passes. No known critical local correctness,
security, privacy, tenant-isolation, or duplicate-send defect remains in the
tested boundaries. Live service and infrastructure proof listed below is a
deployment gate, not inferred from local fixtures.

## Automated campaign

| Check | Result | Evidence |
|---|---|---|
| `npm test` | Pass | 19 files, 68 tests |
| PostgreSQL integration | Pass | 11 tests using the migrated local PostgreSQL database |
| `npm run lint` | Pass | No ESLint findings |
| `npm run typecheck` | Pass | React Router type generation and `tsc --noEmit` |
| `npm run prisma:validate` | Pass | Prisma Client generated; schema valid |
| `npm run build` | Pass | Client and SSR production bundles generated |
| `npm run test:e2e` | Pass | 3 Chromium tests |
| `npx prisma migrate status` | Pass | 11 migrations; schema current |

The PostgreSQL integration suite cleans up its synthetic shops and exercises:

- cross-shop collection-reference rejection;
- same-shop receivable/contact/policy-stage consistency;
- six-way concurrent Shopify webhook receipt deduplication;
- delayed/out-of-order Shopify projection rejection;
- eight-way concurrent reminder reservation with exactly one row;
- independent `REMINDER_SENDS` and `PROVIDER_WEBHOOKS` safety switches;
- customer protected-contact tombstone replay;
- policy-bearing shop purge and domain-hash tombstone replay after simulated
  restore;
- pg-boss adapter stop/start and dead-letter visibility;
- the intended active-receivable indexes and an `EXPLAIN` index plan.

Provider contract tests classify missing configuration and explicit rejection
as definite failures, transport ambiguity as `unknown`, and acceptance only
when Postmark returns a message ID. Existing tests cover aging boundaries,
currency partitioning, queue ordering, promises, entitlement fail-closed
behavior, privacy payload minimization, GraphQL fields/scopes, email header and
template safety, encryption context binding, and CSV formula neutralization.

## Rendered quality review

Browser plugin availability: absent. The approved fallback used Playwright
1.61.1 with installed Chromium.

The flow under test was: `/preview` loads -> primary controls change state ->
the rendered dashboard remains usable and error-free. A second state verified
`/preview?state=unsynced` does not expose fixture balances.

| Browser check | Result |
|---|---|
| Page URL/title and H1 identity | Pass |
| Meaningful non-blank content | Pass |
| Vite/framework overlay absent | Pass |
| Console errors | Pass; none observed |
| Desktop 1440×1000 | Pass |
| Mobile 390×844 | Pass; no document-level horizontal overflow |
| Accessible control names | Pass for buttons, links, inputs, and selects |
| Keyboard focus/activation | Pass |
| Interaction proof | Pass: pause/resume state, company filtering, detail close |
| Unsynchronized-state honesty | Pass; fixture total absent |

Screenshots were generated in the OS temporary directory rather than committed
to the repository.

## Defects fixed during Stage 4

1. Added database triggers for D4 tenant relationships and hardened D5 reminder
   relationship integrity.
2. Recovered concurrent webhook uniqueness races as idempotent duplicates.
3. Prevented an older Shopify `updatedAt` projection from replacing newer
   receivable truth.
4. Corrected the final reminder boundary to use `REMINDER_SENDS` and enforced
   `PROVIDER_WEBHOOKS` independently at provider-event ingestion.
5. Changed the structured logger from runtime object spreading to explicit
   allowlisted field selection.
6. Changed reply-to verification/version deletion from `RESTRICT` to tenant-
   owned cascade so shop privacy purge succeeds with reminder policies.
7. Pinned `brace-expansion` 2.1.4 to remediate GHSA-mh99-v99m-4gvg.

## Security and dependency review

`npm audit --omit=dev --audit-level=high` reports only the six-package React
Router chain for GHSA-qwww-vcr4-c8h2. The upstream advisory says the defect only
affects unstable RSC APIs. This app uses React Router framework mode without
unstable RSC imports or configuration. The only upstream patched version is
8.3.0; npm proposes a forced downgrade to 7.11.0, so no unsafe automatic
downgrade was applied. Re-evaluate during the Stage 5 framework upgrade gate.

Primary advisory records:

- https://github.com/advisories/GHSA-qwww-vcr4-c8h2
- https://github.com/advisories/GHSA-mh99-v99m-4gvg

## Deployment-gated proof

The following cannot be truthfully completed from the local synthetic
environment and must be closed before pilot traffic:

- authenticated embedded Shopify onboarding, dashboard, queue, note, promise,
  policy, statement, billing, and privacy browser journeys;
- Standard-plan behavior plus available Plus partial-payment, negative-balance,
  and protected-customer-data fixtures;
- live Shopify App Pricing free/test/active/frozen/canceled/downgrade states and
  hosted redirects;
- real Postmark sending domain, reply-to delivery, provider webhook ingress,
  bounce/complaint behavior, and ambiguous submission reconciliation;
- alert routing and incident tabletop evidence;
- actual backup creation, isolated restore, full tombstone replay/privacy
  verification, migration rollback/forward-fix, and immutable image rollback.

Safe fallback: live integrations remain fail closed, automation starts paused,
missing provider/billing truth cannot unlock sending, and a restored database
must not serve traffic until deletion replay and privacy verification succeed.
